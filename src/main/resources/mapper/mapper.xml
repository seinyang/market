<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace = "com.example.swagger.mapper.Mapper">
    <insert id="insertUser" parameterType="com.example.swagger.dto.User">
        INSERT INTO market.user (name, id, password, birthday, email)
        VALUES (#{name}, #{id}, #{password} , #{birthday}, #{email})
    </insert>

    <select id="findUserByIdAndPassword" resultType="com.example.swagger.dto.User">
        SELECT name,id FROM market.user WHERE id = #{id} AND password = #{password} LIMIT 1
    </select>

    <select id="findUserById" resultType="com.example.swagger.dto.User">
        SELECT id FROM market.user WHERE email = #{email}
    </select>

<!--    <insert id="findUserByPassword" parameterType="com.example.swagger.dto.User">-->
<!--        INSERT INTO market.user password-->
<!--        VALUES (#{id}, #{email},#{password})-->
<!--    </insert>-->
</mapper>
